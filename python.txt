Assignment: Object-Oriented Shape Area Calculator (Python)
Topic: Classes, Inheritance, and Polymorphism

Objective
In this assignment, you will build a small geometry calculator using Object-Oriented
Programming (OOP) concepts in Python. The program should calculate the area of different
shapes using classes and inheritance.
By completing this assignment, you will practice:
• Creating Python classes
• Using inheritance
• Implementing polymorphism
• Writing methods that perform calculations

Problem Description
You are developing a small geometry module that can compute the area of different shapes.
Create a base class called Shape. Then create the following classes that inherit from Shape:
• Circle
• Square
• Rectangle
• RightTriangle
Each shape class must implement a method called: area()
This method should return the area of the shape based on the dimensions provided.

Base template
Copy this code and edit the required areas adding the necessary code.

import math
class Shape:
def area(self):
"""
This method should be implemented by subclasses
"""
pass
class Circle(Shape):
def __init__(self, diameter):
self.diameter = diameter
def area(self):
# TODO: calculate the circle area
pass
class Square(Shape):
def __init__(self, length):
self.length = length
def area(self):
# TODO: calculate the square area
pass
class Rectangle(Shape):
def __init__(self, length, width):
self.length = length
self.width = width

def area(self):
# TODO: calculate the rectangle area
pass
class RightTriangle(Shape):
def __init__(self, base, height):

self.base = base
self.height = height
def area(self):
# TODO: calculate the triangle area
pass
# Test the program
circle = Circle(10)
square = Square(5)
rectangle = Rectangle(4, 6)
triangle = RightTriangle(3, 8)

print("Circle area:", circle.area())
print("Square area:", square.area())
print("Rectangle area:", rectangle.area())
print("Triangle area:", triangle.area())

Shape Requirements
Your classes should use the following parameters and formulas:
1. Circle
Input parameter: (diameter)
Formula:
Area = π × r²
where r = diameter / 2
2. Square

Input parameter: (length)
Formula:
Area = length²
3. Rectangle
Input parameters: (length, width)
Formula:
Area = length × width
4. Right-Angled Triangle
Input parameters: (base, height)
Formula:
Area = (base × height) / 2

Expected Output (Example)
Your output may look similar to this:
Circle area: 78.53
Square area: 25
Rectangle area: 24
Triangle area: 12

Submission Requirements
Add the solution to your website.

Your program should:
• Run without errors
• Correctly calculate the area for all shapes
• Demonstrate the use of inheritance and polymorphism
Good luck and enjoy building your geometry calculator.

